<!DOCTYPE html>
<html lang="en">
<head>
    <title>TinyApp</title>
</head>
<body>
    <%include partials/_header%>
    <p>List of URLs</p>

        <ul>
        <% for(url in urls) { %>

            <li>
                <%= url %> : <%= urls[url] %>
            </li>

        <% } %>
        </ul>

        <a>Shorten URL</a>

</body>
</html>